# 🤗 CONFIGURACIÓN DE HUGGING FACE
# ===============================
# Archivo de configuración para la publicación automática de datasets

# REPOSITORIO DE DESTINO
# ----------------------
# Formato: usuario_o_organizacion/nombre_repositorio
HF_REPO_NAME="ale-neuratek/wikidump"

# CONFIGURACIÓN DE PRIVACIDAD
# ---------------------------
# true = repositorio privado, false = repositorio público
HF_PRIVATE_REPO=true

# DIRECTORIO DE DATOS POR DEFECTO
# -------------------------------
# Directorio donde se encuentran los archivos JSONL generados
DEFAULT_DATA_DIR="wiki-datasets"

# CONFIGURACIÓN DE METADATOS
# --------------------------
# Información que aparecerá en el README del dataset
DATASET_TITLE="Wikipedia Spanish Dataset"
DATASET_DESCRIPTION="Dataset de Wikipedia en español procesado automáticamente para entrenamiento de modelos de lenguaje"
DATASET_LANGUAGE="es"
DATASET_LICENSE="CC BY-SA 3.0"

# CONFIGURACIÓN DE UPLOAD
# -----------------------
# Límite de tamaño por archivo (MB) - archivos mayores se subirán con LFS
LFS_THRESHOLD_MB=100

# Número máximo de archivos a procesar en paralelo
MAX_PARALLEL_UPLOADS=5

# CONFIGURACIÓN DE AUTENTICACIÓN
# ------------------------------
# Ubicación del token de Hugging Face (se crea automáticamente)
HF_TOKEN_PATH="$HOME/.cache/huggingface/token"

# CONFIGURACIÓN DE LOGGING
# ------------------------
# Nivel de detalle en los logs (INFO, DEBUG, WARNING, ERROR)
LOG_LEVEL="INFO"

# Archivo de log para la publicación
LOG_FILE="hf_upload.log"

# CONFIGURACIÓN AVANZADA
# ----------------------
# Timeout para operaciones de red (segundos)
NETWORK_TIMEOUT=300

# Número máximo de reintentos en caso de error
MAX_RETRIES=3

# Tiempo de espera entre reintentos (segundos)
RETRY_DELAY=5

# ETIQUETAS PARA EL DATASET
# -------------------------
# Etiquetas que aparecerán en Hugging Face para facilitar la búsqueda
DATASET_TAGS="wikipedia,spanish,conversations,training-data,nlp"

# CONFIGURACIÓN DE README
# -----------------------
# Incluir estadísticas detalladas en el README
INCLUDE_DETAILED_STATS=true

# Incluir ejemplos de uso en el README
INCLUDE_USAGE_EXAMPLES=true

# Incluir información de categorías en el README
INCLUDE_CATEGORY_INFO=true

# NOTAS:
# ------
# - Modifica HF_REPO_NAME para cambiar el repositorio de destino
# - Asegúrate de tener permisos de escritura en el repositorio
# - El token se configurará automáticamente en el primer uso
# - Los archivos grandes (>LFS_THRESHOLD_MB) se manejarán con Git LFS automáticamente
